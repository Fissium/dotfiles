local null_ls = require "null-ls"

local b = null_ls.builtins

local home = vim.fn.expand "$HOME"

local sources = {

  -- Lua
  b.formatting.stylua,
  -- Python
  b.formatting.ruff.with {
    command = home .. "/.local/share/nvim/mason/packages/ruff-lsp/venv/bin/ruff",
    args = {
      "format",
      "-n",
      "--stdin-filename",
      "$FILENAME",
      "-",
      "--line-length=88",
    },
  },
  -- Bash
  b.formatting.shfmt,
  b.diagnostics.shellcheck,
  -- YAML, Markdown
  b.formatting.yamlfmt,
  -- Docker
  b.diagnostics.hadolint,
  -- TOML
  b.formatting.taplo,
}

null_ls.setup {
  debug = true,
  sources = sources,
}
